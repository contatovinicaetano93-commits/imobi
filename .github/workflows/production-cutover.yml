# DESATIVADO — legado (branch claude/*, URLs AWS antigas).
# Cutover de produção: ver docs/DEPLOY_STACK.md + tag v* no main.
# Para reativar: reescrever com URLs canônicas Render/Vercel e ref main.

name: Production Cutover (disabled)

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Workflow disabled
        run: |
          echo "Este workflow está desativado."
          echo "Stack canônica: Vercel (web) + Render (API) na branch main."
          echo "Ver: docs/DEPLOY_STACK.md"
          exit 1
