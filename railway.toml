[build]
builder = "DOCKERFILE"
dockerfilePath = "services/api/Dockerfile"

[deploy]
startCommand = "pnpm --filter @imbobi/api start:railway"
healthcheckPath = "/api/v1/health/live"
restartPolicyType = "ON_FAILURE"
