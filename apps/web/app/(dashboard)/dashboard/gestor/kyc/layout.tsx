import type { Metadata } from "next";

export const metadata: Metadata = { title: "KYC — Gestor do Fundo" };

export default function KycLayout({ children }: { children: React.ReactNode }) {
  return children;
}
