import type { Metadata } from "next";

export const metadata: Metadata = { title: "Etapas Pendentes — Gestor do Fundo" };

export default function EtapasLayout({ children }: { children: React.ReactNode }) {
  return children;
}
