import type { Metadata } from "next";

export const metadata: Metadata = { title: "Etapas Pendentes — Gestor IMOBI" };

export default function EtapasLayout({ children }: { children: React.ReactNode }) {
  return children;
}
