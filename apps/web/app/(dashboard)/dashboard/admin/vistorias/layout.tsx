import type { Metadata } from "next";

export const metadata: Metadata = { title: "Vistorias — Admin IMOBI" };

export default function AdminVistoriasLayout({ children }: { children: React.ReactNode }) {
  return children;
}
