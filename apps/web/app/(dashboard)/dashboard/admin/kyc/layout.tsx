import type { Metadata } from "next";

export const metadata: Metadata = { title: "KYC — Admin IMOBI" };

export default function AdminKycLayout({ children }: { children: React.ReactNode }) {
  return children;
}
