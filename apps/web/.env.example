# ── Next.js Web Frontend ───────────────────────────
# API Backend URL (must match API server CORS_ORIGIN)
NEXT_PUBLIC_API_URL=http://localhost:4000

# Sentry (error tracking) - Optional
# NEXT_PUBLIC_SENTRY_DSN=

# Environment
NODE_ENV=development
