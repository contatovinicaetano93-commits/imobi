{
  "buildCommand": "turbo run build --filter=@imbobi/web --filter=@imbobi/api",
  "installCommand": "pnpm install",
  "rewrites": [
    {
      "source": "/api/:match*",
      "destination": "/api/handler"
    }
  ]
}
